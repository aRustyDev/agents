# AI Components

The files in these sub-directories are all component files, used to help create 'plugin' objects for publishing.

- Each 'plugin' acts as a consumer of the components.
  - Consumes via `just install` applied recursively

All plugins are tested using `just test`
